<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="RoyalBakeryCashier.Pages.CashierPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    BackgroundColor="{StaticResource PageBg}"
    Title="POS Window">

    <Grid RowDefinitions="Auto,*"
          ColumnDefinitions="3*,1*"
          Padding="15"
          ColumnSpacing="10">

        <!-- ================= TOP BAR ================= -->
        <Grid Grid.ColumnSpan="2" Padding="0,0,0,10">
            <HorizontalStackLayout HorizontalOptions="End"/>
        </Grid>

        <!-- ================= LEFT SIDE ================= -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="15">

                <Label Text="Cashier POS"
                       FontSize="28"
                       TextColor="White"
                       HorizontalOptions="Center"/>

                <!-- Categories -->
                <Frame BackgroundColor="{StaticResource CardBg}"
                       Padding="10"
                       CornerRadius="12"
                       BorderColor="{StaticResource Gray600}">

                    <ScrollView Orientation="Horizontal">
                        <HorizontalStackLayout
                            x:Name="CategoryStack"
                            Spacing="12"/>
                    </ScrollView>
                </Frame>

                <!-- Items -->
                <Frame BackgroundColor="{StaticResource CardBg}"
                       Padding="10"
                       CornerRadius="12"
                       BorderColor="{StaticResource Gray600}">

                    <VerticalStackLayout Spacing="10">

                        <Label Text="Items"
                               FontSize="20"
                               TextColor="White"/>

                        <!-- FIXED: Added Orientation -->
                        <CollectionView x:Name="ItemsCollectionView"
                                        SelectionMode="Single"
                                        SelectionChanged="ItemsCollectionView_SelectionChanged">

                            <CollectionView.ItemsLayout>
                                <GridItemsLayout Orientation="Vertical" Span="5"/>
                            </CollectionView.ItemsLayout>

                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Frame Padding="8"
                                           BackgroundColor="{StaticResource ItemRowBg}"
                                           CornerRadius="10"
                                           BorderColor="{StaticResource Gray600}">

                                        <VerticalStackLayout Spacing="6">
                                            <Image Source="items.png"
                                                   HeightRequest="90"
                                                   WidthRequest="90"
                                                   HorizontalOptions="Center"/>

                                            <Label Text="{Binding Name}" FontSize="14" TextColor="White"/>

                                            <Label Text="{Binding Price, StringFormat='LKR {0:F2}'}"
                                                   FontSize="12" TextColor="{StaticResource TotalHighlight}"/>

                                            <Label Text="{Binding AvailableStock, StringFormat='Stock: {0}'}"
                                                   FontSize="10" TextColor="{StaticResource Gray300}"/>
                                        </VerticalStackLayout>

                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>

                        </CollectionView>

                    </VerticalStackLayout>
                </Frame>

            </VerticalStackLayout>
        </ScrollView>

        <!-- ================= RIGHT SIDE (RESPONSIVE CART) ================= -->
        <Frame Grid.Row="1"
               Grid.Column="1"
               BackgroundColor="{StaticResource CardBg}"
               Padding="10"
               CornerRadius="12"
               BorderColor="{StaticResource Gray600}">

            <Grid RowDefinitions="Auto,*,Auto,Auto,Auto"
                  RowSpacing="10">

                <!-- Title -->
                <Label Text="Bill"
                       FontSize="20"
                       TextColor="White"
                       HorizontalOptions="Center"/>

                <!-- Cart fills available space and scrolls -->
                <CollectionView Grid.Row="1"
                                x:Name="CartCollectionView"
                                VerticalOptions="FillAndExpand">

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame Padding="8"
                                   Margin="0,4"
                                   BackgroundColor="{StaticResource ItemRowBg}"
                                   CornerRadius="8"
                                   BorderColor="{StaticResource Gray600}">

                                <Grid ColumnDefinitions="*,Auto,Auto">

                                    <Label Text="{Binding Name}"
                                           TextColor="White"
                                           VerticalOptions="Center"/>

                                    <HorizontalStackLayout Grid.Column="1"
                                                           Spacing="4">

                                        <Button Text="-"
                                                WidthRequest="30"
                                                HeightRequest="30"
                                                BackgroundColor="{StaticResource CatSpecials}"
                                                TextColor="White"
                                                Clicked="DecreaseQuantity_Clicked"
                                                CommandParameter="{Binding .}"/>

                                        <Label Text="{Binding Quantity}"
                                               WidthRequest="25"
                                               TextColor="White"
                                               HorizontalTextAlignment="Center"/>

                                        <Button Text="+"
                                                WidthRequest="30"
                                                HeightRequest="30"
                                                BackgroundColor="{StaticResource PlaceOrderBtn}"
                                                TextColor="White"
                                                Clicked="IncreaseQuantity_Clicked"
                                                CommandParameter="{Binding .}"/>
                                    </HorizontalStackLayout>

                                    <Label Grid.Column="2"
                                           Text="{Binding Total, StringFormat='LKR {0:F2}'}"
                                           TextColor="{StaticResource TotalHighlight}"
                                           HorizontalOptions="End"/>
                                </Grid>

                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>

                </CollectionView>

                <!-- Total -->
                <HorizontalStackLayout Grid.Row="2"
                                       HorizontalOptions="End"
                                       Spacing="10">

                    <Label x:Name="TotalLabel" Text="Total: LKR 0.00" TextColor="{StaticResource TotalHighlight}" FontSize="16" FontAttributes="Bold"/>

                    <Button Text="Place Order"
                            BackgroundColor="{StaticResource PlaceOrderBtn}"
                            TextColor="White"
                            Clicked="PlaceOrder_Clicked"/>
                </HorizontalStackLayout>

                <!-- Quantity -->
                <Entry Grid.Row="3"
                       x:Name="QuantityEntry"
                       Placeholder="Type quantity then tap item"
                       PlaceholderColor="{StaticResource Gray400}"
                       TextColor="White"
                       BackgroundColor="{StaticResource InputBg}"
                       IsReadOnly="True"
                       Keyboard="Numeric"/>

                <!-- KEYPAD -->
                <Grid Grid.Row="4"
                      RowDefinitions="*,*,*,*"
                      ColumnDefinitions="*,*,*"
                      ColumnSpacing="5"
                      RowSpacing="5">

                    <Button Text="1" BackgroundColor="{StaticResource NumpadBg}" TextColor="{StaticResource NumpadText}" Clicked="Keypad_Clicked"/>
                    <Button Grid.Column="1" Text="2" BackgroundColor="{StaticResource NumpadBg}" TextColor="{StaticResource NumpadText}" Clicked="Keypad_Clicked"/>
                    <Button Grid.Column="2" Text="3" BackgroundColor="{StaticResource NumpadBg}" TextColor="{StaticResource NumpadText}" Clicked="Keypad_Clicked"/>

                    <Button Grid.Row="1" Text="4" BackgroundColor="{StaticResource NumpadBg}" TextColor="{StaticResource NumpadText}" Clicked="Keypad_Clicked"/>
                    <Button Grid.Row="1" Grid.Column="1" Text="5" BackgroundColor="{StaticResource NumpadBg}" TextColor="{StaticResource NumpadText}" Clicked="Keypad_Clicked"/>
                    <Button Grid.Row="1" Grid.Column="2" Text="6" BackgroundColor="{StaticResource NumpadBg}" TextColor="{StaticResource NumpadText}" Clicked="Keypad_Clicked"/>

                    <Button Grid.Row="2" Text="7" BackgroundColor="{StaticResource NumpadBg}" TextColor="{StaticResource NumpadText}" Clicked="Keypad_Clicked"/>
                    <Button Grid.Row="2" Grid.Column="1" Text="8" BackgroundColor="{StaticResource NumpadBg}" TextColor="{StaticResource NumpadText}" Clicked="Keypad_Clicked"/>
                    <Button Grid.Row="2" Grid.Column="2" Text="9" BackgroundColor="{StaticResource NumpadBg}" TextColor="{StaticResource NumpadText}" Clicked="Keypad_Clicked"/>

                    <Button Grid.Row="3" Text="0" BackgroundColor="{StaticResource NumpadBg}" TextColor="{StaticResource NumpadText}" Clicked="Keypad_Clicked"/>

                    <Button Grid.Row="3" Grid.Column="1"
                            Text="Clear"
                            BackgroundColor="{StaticResource ClearBtn}"
                            TextColor="White"
                            Clicked="ClearKeypad_Clicked"/>

                    <Button Grid.Row="3" Grid.Column="2"
                            Text="CLR CART"
                            BackgroundColor="{StaticResource ClearCartBtn}"
                            TextColor="{StaticResource NumpadText}"
                            Clicked="ClearCart_Clicked"/>
                </Grid>

            </Grid>

        </Frame>

    </Grid>
</ContentPage>